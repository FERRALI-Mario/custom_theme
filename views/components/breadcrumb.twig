<nav class="text-base text-gray-500 mb-4" aria-label="Breadcrumb">
	<ol class="list-reset flex flex-wrap items-center space-x-1">
		{% for item in breadcrumb %}
			<li>
				<a href="{{ item.url }}" class="text-gray-800 font-semibold hover:underline">{{ item.title }}</a>
				{% if item.url and not loop.last %}
					<span class="mx-1">›</span>
				{% endif %}
			</li>
		{% endfor %}
	</ol>
</nav>
