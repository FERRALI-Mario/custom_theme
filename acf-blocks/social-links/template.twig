<section class="py-4 px-4">
    <div class="container mx-auto text-center">
        {% if fields.social_links %}
        <div class="flex justify-center gap-4">
            {% for link in fields.social_links %}
                <a href="{{ link.url }}" target="_blank" aria-label="Visit {{ link.url }}" class="text-gray-600 hover:text-gray-900">
                    {% if link.icon == 'facebook' %}
                        <i class="fab fa-facebook text-2xl"></i>
                    {% elseif link.icon == 'instagram' %}
                        <i class="fab fa-instagram text-2xl"></i>
                    {% elseif link.icon == 'snapchat' %}
                        <i class="fab fa-snapchat-ghost text-2xl"></i>
                    {% elseif link.icon == 'tiktok' %}
                        <i class="fab fa-tiktok text-2xl"></i>
                    {% elseif link.icon == 'pinterest' %}
                        <i class="fab fa-pinterest text-2xl"></i>
                    {% elseif link.icon == 'github' %}
                        <i class="fab fa-github text-2xl"></i>
                    {% elseif link.icon == 'linkedin' %}
                        <i class="fab fa-linkedin text-2xl"></i>
                    {% endif %}
                </a>
            {% endfor %}
        </div>
        {% else %}
            <p>Aucun lien social disponible.</p>
        {% endif %}
    </div>
</section>
